'use babel';

import AtomBuildStarwebView from '../lib/atom-build-starweb-view';

describe('AtomBuildStarwebView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
